# Copy to .env (gitignored) and fill in your key.
# Used by packages/evals/ — the eval harness loads from <repo-root>/.env via dotenv.
# Existing process.env values take precedence; this file is the fallback.

ANTHROPIC_API_KEY=sk-ant-...
