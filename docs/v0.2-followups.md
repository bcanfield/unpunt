# un-punt v0.2 follow-ups

> Authoritative TODO list for everything we know about but haven't shipped. Captured at the close of the v0.2 dogfood + fix1 cycle (May 2026) so nothing gets lost as we move forward. Companion to [`v0.2-dogfood-report.md`](v0.2-dogfood-report.md).
>
> **How to read this doc**: items are grouped by *when* they should land (pre-launch, v0.2.x patches, v0.3 next major, backlog). Each item lists its **source** (which session / finding generated it), **effort estimate** when known, **status** (open / in-progress / blocked), and **rationale** for the priority.
>
> When you finish an item, mark it complete inline (don't delete) so the audit trail stays.

---

## Pre-launch (must land before tagging v0.2.0)

### PL-1 — Push `origin/main` for the most recent fix1 commits

- **Status**: open
- **Source**: this session (May 4 wrap-up)
- **Effort**: 5 sec
- **Rationale**: commits `b3b1303`, `4096e6e`, `bb478bb`, plus this doc + cleanup pass land locally; need to be public before launch.
- **Action**: `git push origin main`

### PL-2 — Update `README.md` per `docs/v0.2-launch-readme-draft.md`

- **Status**: open
- **Source**: Q5c launch readiness; draft committed at commit `e45d424`
- **Effort**: ~30 min (one focused session)
- **Rationale**: README still pitches v0.1 architecture. The draft has a section-by-section diff plan including a NEW credibility-builder section ("We built it. We dogfooded it. We found the foundation issue. We fixed it."). High-leverage launch asset.
- **Action**: open the draft, apply each section's diff to `README.md` directly, commit as a single "README: v0.2 launch — Decision #21 hooks shipped" change.

### PL-3 — Tag `v0.2.0` + push tag

- **Status**: blocked on PL-2
- **Effort**: 30 sec
- **Rationale**: locks the launch commit as a versioned release.
- **Action**: `git tag v0.2.0 && git push --tags`

### PL-4 — Announcement post

- **Status**: blocked on PL-3
- **Effort**: ~1 hr drafting + posting
- **Rationale**: HN, Bluesky, existing channels. Lead with the Decision #21 dogfood arc — *"we built it, used it ourselves, found the foundation issue, fixed it in public"* — strongest credibility builder for AI dev tools in 2026. See [`launch-plan.md`](launch-plan.md) for venue strategy + timing skeleton.
- **Action**: draft on personal blog (canonical), mirror to dev.to / Hashnode, then HN Show post + Bluesky thread + Reddit per launch-plan.

### PL-5 — Optional: punt-board deployed publicly (the showcase asset)

- **Status**: open in punt-board repo
- **Source**: Q5c original brief; deploy prompt drafted in this session's wrap-up
- **Effort**: 1 afternoon (single Claude Code session via the deploy prompt)
- **Rationale**: deployed punt-board becomes the un-punt landing-page hero asset. Nice-to-have for launch; not strictly blocking. If skipped, README's hero is just the install GIF + skill demo.

---

## v0.2.x patches (small additive changes, post-launch, prioritized)

### PATCH-1 — Cursor adapter (`adapters/cursor/`)

- **Status**: open
- **Source**: Q5c future-lift estimates; Q2b Cursor research
- **Effort**: ~80 LOC + ~2 hrs testing (single afternoon)
- **Rationale**: Cursor 2.4 (Jan 2026) shipped `SKILL.md` open standard — **same skill body ports unchanged**. Only adapter manifest + `.cursor/hooks.json` registration differs. Hook scripts at `core/hooks/` are reused as-is. Highest-ROI cross-platform expansion.
- **Action sketch**: copy `core/skill/SKILL.body.md` → `adapters/cursor/skills/un-punt/SKILL.md` via `core/build.sh` extension; create `adapters/cursor/.cursor/hooks.json` with `sessionStart` / `afterFileEdit` (Cursor's PostToolUse equivalent — Q2b notes this is *cleaner* than Claude Code's matcher) / `beforeSubmitPrompt` registrations; add `un-punt install cursor` CLI subcommand.
- **Validation**: install in a Cursor-using punt-board copy; smoke-test the same Probes 9–12.

### PATCH-2 — Codex adapter (`adapters/codex/`)

- **Status**: open
- **Source**: Q5c future-lift estimates; Q2a Codex research
- **Effort**: ~70 LOC + ~2 hrs testing
- **Rationale**: Codex CLI 0.124.0 (April 2026) hooks stable + same SKILL.md standard + plugin manifest auto-discovers `hooks/hooks.json`. Codex's `Stop` semantics actually *better* than Claude Code's for the wrap-up offer.
- **Action sketch**: `adapters/codex/.codex-plugin/plugin.json` declaring un-punt; `adapters/codex/skills/un-punt/SKILL.md` (built from same source); `adapters/codex/hooks/hooks.json` registering hooks.

### PATCH-3 — `validate-v0.2.sh` cross-platform check (Cursor + Codex install verification)

- **Status**: blocked on PATCH-1, PATCH-2
- **Effort**: ~30 LOC additions to existing script
- **Rationale**: when adapters land, the validation script should verify each platform's install state.

### PATCH-4 — Decision 13 forward-pointer comment

- **Status**: open
- **Source**: Q3c flagged this as a cleanup; methodology audit suggested codifying it
- **Effort**: 10 min
- **Rationale**: Decision 13 is left as written but partially superseded by #21. Future readers may not realize. A short forward-pointer comment under Decision 13 noting *"superseded for capture/suggestion paths by Decision #21 (May 2026); bullets 1 + 4 still hold"* prevents confusion.

### PATCH-5 — Top-3-areas dedupe in cold-start summary

- **Status**: open
- **Source**: Q8a finding #2 (Probe 5 minor finding from v0.1); deferred at user gate
- **Effort**: ~10 LOC in cold-start procedure (`core/skill/snippets/cold-start.md`)
- **Rationale**: cold-start "Top 3 areas" double-counts nested path prefixes (8+8+4=20 ≠ 18 items). Cosmetic; user can read individual items for truth. Bundle with other cold-start polish.

### PATCH-6 — Refused-section enumeration covers all 12 rules

- **Status**: open
- **Source**: Q8a finding #4 (Probe 5 minor finding); deferred at user gate
- **Effort**: ~20 LOC in `core/skill/SKILL.body.md` sweep planning section
- **Rationale**: `plan.md` Refused section walks 7 of 12 rules explicitly. Doc-quality / auditability — reader can't easily verify rules 1, 4, 9, 10, 11 had been checked. Update planning template to enumerate all 12.

### PATCH-7 — Hook execution observability layer

- **Status**: open
- **Source**: v0.2.0-fix1 lesson — silent failures are the worst
- **Effort**: ~50 LOC across hooks + a debug doc
- **Rationale**: the May 4 `$HOME` bug failed silently. If a similar bug recurs, we want fast detection. Two cheap mechanisms:
  - Hooks emit a no-op debug line to stderr if `$DEBUG_UN_PUNT` env var is set, so users can opt into verbose logging
  - `validate-v0.2.sh` includes a "fire each hook with a synthetic payload, verify expected JSON output" check
- **Action**: add the debug-line to each hook script (5 LOC each); add the synthetic-fire check to validate-v0.2.sh (~30 LOC).

### PATCH-8 — Unmute the dogfood-log.md gitignore in punt-board (or keep tracked)

- **Status**: open in punt-board repo
- **Source**: side-finding from this session (line 50 in punt-board's `.gitignore` is commented out)
- **Effort**: 30 sec
- **Rationale**: brief expected `dogfood-log.md` to be gitignored; user has it tracked. **Now that the canonical dogfood record lives at `docs/v0.2-dogfood-report.md` in this repo, the punt-board log can be safely deleted or gitignored** — it's no longer the source of truth.

---

## v0.3 (next major — depends on v0.2 evidence)

**Moved to its own doc for prominence**: see [`v0.3-roadmap.md`](v0.3-roadmap.md).

Summary of v0.3 work items (full details in the roadmap):

- **V03-1** — Sketch (iv) `type: "prompt"` hooks (escape hatch; gated on Sketch (ii) compliance < 60%)
- **V03-2** — Line-drift recheck mechanism (`last_verified_at:` field + PostToolUse-driven recheck)
- **V03-3** — Convert dogfood-log misses to formal `core/golden-set/` scenarios
- **V03-4** — Eval harness refresh for hook-driven captures

Three triggers can open v0.3 work: compliance drop, stability achieved, or external (Claude Code update / competitor pressure). Until one fires, v0.2 stays in maintenance and v0.3 work is documented-but-deferred.

---

## Backlog (nice-to-have, no urgency)

### BL-1 — Copilot CLI Tier 2 adapter

- **Source**: Q2c tier strategy
- **Effort**: ~30 LOC + tier doc
- **Rationale**: Copilot CLI's PostToolUse can't modify result + UserPromptSubmit is read-only on CLI; only SessionStart + PreToolUse-deny supported. Tier 2 partial coverage. Skip auto-capture-on-defer; rely on user invoking `/un-punt` manually.

### BL-2 — Aider Tier 3 adapter (primer-only)

- **Source**: Q2c tier strategy
- **Effort**: ~10 LOC + tier doc
- **Rationale**: Aider has no general-purpose hooks. Ship `read: AGENTS.md` snippet for `.aider.conf.yml`. No dynamic refusal, no auto-capture, no sweep prompt — primer carries everything.

### BL-3 — Gemini CLI adapter

- **Source**: Q2c
- **Effort**: ~40 LOC + 2 hrs testing
- **Rationale**: Gemini CLI hooks GA'd; arguably *richer* hook surface than Claude Code (BeforeModel/AfterModel/BeforeToolSelection are bonus). Bundle un-punt as a Gemini Extension for one-command install.

### BL-4 — `frontend-design`-driven landing page

- **Source**: this session's "what's next" musing
- **Effort**: 1 day
- **Rationale**: shadcn skill + frontend-design skill available; could iterate punt-board into a polished landing-page-style showcase.

### BL-5 — Evaluate `when_to_use` and `paths` frontmatter mitigations

- **Source**: Q5b — Candidate D dominated; available as v0.3 add-on if needed
- **Effort**: ~30 min experiment
- **Rationale**: Decision 13 April 2026 amendment listed these as low-cost description-match-strengthening fields. v0.2 didn't include them (Q5b judged dominated in presence of hooks). Worth a small experiment post-launch to see if they materially improve description-match auto-loading rate alongside hooks. Cheap to add; cheap to remove if no benefit.

### BL-6 — `/un-punt promote <id>` slash command

- **Source**: Q8a finding #3 implication — current confidence-promotion path is documentation only
- **Effort**: ~50 LOC
- **Rationale**: documentation-only confidence promotion (the v0.2 fix for Q8a #3) requires the agent to remember the procedure and apply it. If real users routinely ignore the docs, automate it via a slash command that walks the user through item review + threshold raise + lifecycle row append.

### BL-7 — Multi-developer support / team aggregation

- **Source**: Decision 12 (single-developer-per-repo at MVP); Phase 3+
- **Effort**: substantial
- **Rationale**: out of scope for v0.2; flagged in original build plan.

### BL-8 — MCP server for team-aggregation surface

- **Source**: Decision 15 (deferred unless team aggregation becomes the primary use case)
- **Status**: not warranted by current evidence

### BL-9 — Eng-leader dashboard (debt-velocity over time)

- **Source**: Phase 4 of original build plan
- **Status**: out of scope for foreseeable future

---

## Status legend

- **open** — work to do; no blocker
- **blocked on \<X\>** — blocked on completion of another item
- **in-progress** — actively working on it
- **done** — completed (mark inline; don't delete)
- **deferred** — explicit decision to not do; cite session / decision

---

## Living section: items completed since v0.2 ship

> Append rows as items complete. Don't delete completed items above; mark them done in-place AND add a row here for at-a-glance progress.

| Item | Date | Commit | Notes |
|---|---|---|---|
| *(empty — no items completed yet post-v0.2)* | | | |
